import classes from './ProdItemForm.module.css'

const ProdItemForm = props => {
  return (
    <form className={classes.form}>
      <input></input>
      <button>+ Add</button>
    </form>
  );
};

export default ProdItemForm;